import React from "react";

const Button = () => {
  return (
    <button className="h-10 px-6 font-semibold rounded-md bg-blue-500 hover:bg-blue-700 text-white">
      button
    </button>
  );
};

export default Button;
